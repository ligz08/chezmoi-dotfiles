# Bash aliases - Generated by chezmoi from .chezmoidata.toml
# Do not edit directly; modify .chezmoidata.toml instead

{{- range .aliases }}
  {{- if not (and (hasKey . "pwsh_only") .pwsh_only) }}
    {{- if not (and (hasKey . "fish_only") .fish_only) }}
alias {{ .alias }}='{{ .command }}'
    {{- end }}
  {{- end }}
{{- end }}
