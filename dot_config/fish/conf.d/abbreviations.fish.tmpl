# Fish abbreviations - Generated by chezmoi from .chezmoidata.toml
# Do not edit directly; modify .chezmoidata.toml instead

{{- range .aliases }}
  {{- if not (and (hasKey . "pwsh_only") .pwsh_only) }}
    {{- if not (and (hasKey . "bash_only") .bash_only) }}
abbr -a {{ .alias }} '{{ .command }}'
    {{- end }}
  {{- end }}
{{- end }}
